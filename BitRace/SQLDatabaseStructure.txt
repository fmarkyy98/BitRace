TABLE Questions 
	id INT PRIMARY KEY
	difficulty VARCHAR(255)
	text VARCHAR(255);

TABLE Ansvers
	id INT Half-PRIMARY KEY
	question_id INT Half-PRYMARY KEY FOREIGN KEY question.id
	text VARCHAR(255)
	is_correct_ansver BOOLEAN

TABLE Players
	id INT PRIMARY KEY
	name VARCHAR(255)
	score INT

Babucs.Hajnalka@njszg.hu